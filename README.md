Implemented an in-memory cache in Java supporting constant-time get and put operations.

Designed LRU (Least Recently Used) eviction using a combination of HashMap and Doubly Linked List.

Added TTL (Time To Live) support for automatic expiration of entries using a PriorityQueue.

Handled cache eviction and expiration efficiently without background threads.

Focused on clean object-oriented design .
